/**
 * Tests del App. Para probar la app completa con rutas, crear un test
 * que importe MemoryRouter desde react-router-dom y envuelva <App />.
 */
test('suite App cargado', () => {
  expect(true).toBe(true);
});
